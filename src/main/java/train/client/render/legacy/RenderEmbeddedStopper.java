package train.client.render.legacy;

import train.client.render.RenderStopper;
import train.common.library.track.EnumTracks;

public class RenderEmbeddedStopper extends RenderStopper
{
    public RenderEmbeddedStopper(EnumTracks enumTrack)
    {
        super(enumTrack);
    }
}
